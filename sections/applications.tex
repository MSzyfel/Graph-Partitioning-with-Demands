\section{Applications}

Below we show few applications of the \textsc{Minimum Generalized Conductance Problem}. We show how to use the approximation algorithm for the \textsc{Minimum Generalized Conductance Problem} to obtain approximation algorithms for the \textsc{Quadratic Sparsest Cut}, \textsc{Graph Partitioning with Demands} and \textsc{Hierarchical Clustering with Demands} problems.
\subsection{Warm up: Quadratic Sparsest Cut}

In the problem we call the \textsc{Quadratic Sparsest Cut}, one is required to find a subset of vertices $S$ that minimizes 
\[\frac{c\br{S, \bar{S}}}{\br{\spr{S}\cdot \spr{\bar{S}}}^2}.\]

Intuitively, this objective forces the solution to be more balanced. Observe, that if one assigns each pair of vertices $u,v$ a demand of $w\br{u,v}=1$, then we have that 
\[w\br{S, V}\cdot w\br{\bar{S}, V}=\br{\spr{S}+\spr{S}\cdot \spr{\bar{S}}}\cdot \br{\spr{\bar{S}}+\spr{S}\cdot \spr{\bar{S}}}=\spr{S}\cdot \spr{\bar{S}}+\spr{S}^2\cdot \spr{\bar{S}}+\spr{S}\cdot \spr{\bar{S}}^2+\br{\spr{S}\cdot \spr{\bar{S}}}^2.
\] 
Since $\spr{S},\spr{\bar{S}}\geq 1$, we get that 
\[\br{\spr{S}\cdot \spr{\bar{S}}}^2\leq w\br{S, V}\cdot w\br{\bar{S}, V}\leq 4\cdot\br{\spr{S}\cdot \spr{\bar{S}}}^2
\]
and the two objectives are equivalent up to a factor of $4$. Since $w$ is multiplicative, we obtain the following corollary:
\begin{corollary}
    There is a polynomial-time $O(\sqrt{\log n})$-approximation algorithm for the \textsc{Quadratic Sparsest Cut Problem}.
\end{corollary}

\subsection{Graph Partitioning with Demands}
Below, we show how to use the approximation algorithm for the \textsc{Minimum Generalized Conductance Problem} to obtain an approximation algorithm for \textsc{Graph Partitioning with Demands}. In this problem, we are given a graph $G=(V,E,c,w)$ and a fixed constant $\rho\in(0,1)$, and the goal is to find a subset of edges $C\subseteq E$ that minimizes $c\br{C}$ such that for every $H\in V-C$, we have $w\br{H}\leq \rho\,w\br{V}$.

Define
\[
\delta_{\rho}:=\min\brc{1/4,1-\rho}.
\]
We exhibit a bi-criteria approximation algorithm with an additional constant parameter $\eta$. For any
\[
\eta\in\br{\max\brc{1/2,1-\delta_{\rho}},1},
\]
the algorithm returns a subset $C$ such that $c\br{C}=O\br{\gamma/(\eta+\delta_{\rho}-1)^2}\cdot c\br{C^*}$ and for every $H\in V-C$, we have $w\br{H}\leq \eta w\br{V}$, where $C^*$ is the optimal solution and $\gamma$ is the approximation ratio for the \textsc{Minimum Generalized Conductance Problem}. The analysis uses a generalization of the standard technique of dealing with balanced cut/graph partitioning problems using \textsc{Sparsest Cut} \cite{ExpanderFlowsGeometricEmbeddingsAndGraphPartitioning,ExpanderFlowsGeometricEmbeddingsAndGraphPartitioning}. \DD{$\leftarrow$ tutaj mialo byc cos innego docytowane, czy ma byc jeden cite?} However, due to the fact that we need to control both the demand in the components and the demand crossing the cut, a more careful analysis is required. The algorithm is simple:
\begin{enumerate}
    \item $C\gets\emptyset$
    \item $G'\gets \br{V, E,c,w}$
    \item While $w\br{V\br{G'}}> \eta w\br{V}$ do
    \begin{itemize}
        \item Use the $\gamma$-approximation algorithm for the \textsc{Minimum Generalized Conductance Problem} to find a cut $(S,\bar{S})$ in $G'$, where $w\br{S}\leq w\br{\bar{S}}$.
        \item $C\gets C\cup E\br{S, \bar{S}}$
        \item Remove the vertices in $S$ from $G'$, i.e., $G'\gets G'[V\br{G'}\setminus S]$
    \end{itemize}
    \item Return $C$
\end{enumerate}

The above algorithm is characterized by the following claim.
\begin{theorem}\label{thm:gpd-bicriteria}
    For every fixed $\rho\in(0,1)$ and every fixed $\eta\in\br{\max\brc{1/2,1-\delta_{\rho}},1}$, there exists a polynomial-time algorithm that returns, for any input graph, a subset of edges $C$ such that
    \[
    c\br{C}=O\br{\gamma/(\eta+\delta_{\rho}-1)^2}\cdot c\br{C^*}
    \]
    and for every $H\in V-C$, we have
    \[
    w\br{H}\leq \eta w\br{V},
    \]
    where $\delta_{\rho}:=\min\brc{1/4,1-\rho}$, $C^*$ is the optimal solution and $\gamma$ is the approximation ratio for the \textsc{Minimum Generalized Conductance Problem}.
\end{theorem}
\begin{proof}
    We first prove the component-size guarantee. Consider all components $H\in G-C$. If $H$ is $G'$ after the last iteration of the while loop, then by the stopping condition of this loop we have $w\br{H}\leq \eta w\br{V}$. Otherwise, $H$ was the smaller side of the cut in some iteration of the while loop, and thus $w\br{H}\leq w\br{V}/2\leq \eta w\br{V}$, where we use the assumption $\eta\in\br{\max\brc{1/2,1-\delta_{\rho}},1}$ (in particular, $\eta\geq 1/2$). In either case, $w\br{H}\leq \eta w\br{V}$.

    Now we want to reason about $c\br{C}$.
    We partition the components of $G-C^*$ into two vertex sets $A$ and $B$, each inducing enough fraction of the total demand as stated in the next lemma.
    \begin{lemma} \label{lem:ABpartition}
                For every graph $G$ and demand function $w$, there exists a partition of the components of $G-C^*$ into two vertex sets $A$ and $B$ such that $w\br{A, V}\geq \delta_{\rho}w\br{V}$ and $w\br{B, V}\geq \delta_{\rho}w\br{V}$.
    \end{lemma}
    \begin{proof}
    We have the following cases:
    \begin{enumerate}
                \item There exists $H\in G-C^*$ such that $w\br{V_H}\geq \delta_{\rho}w\br{V}$, where $V_H$ is the vertex set of $H$.
        In this case, we can set $A=V_H$ and $B=V\setminus V_H$. We have
                \[w\br{A, V}\geq w\br{A} \geq \delta_{\rho}w\br{V}\]
         and 
                \[w\br{B, V}=w\br{V}-w\br{A}\geq \br{1-\rho}w\br{V}\geq \delta_{\rho}w\br{V}\]
                since $w\br{A}=w\br{H}\leq \rho w\br{V}$.
            The latter is by the fact that $C^*$ is optimal and the condition on each connected component $H$ to satisfy $w(V_H)\leq \rho w(V)$; the last inequality uses the definition $\delta_{\rho}=\min\brc{1/4,1-\rho}$.
                \item For every $H\in G-C^*$, we have $w\br{H}\leq \delta_{\rho}w\br{V}$. We proceed in iterations. Start with $A=\emptyset$ and keep adding vertices of components of $G-C^*$ to $A$ until $w\br{A}\geq \delta_{\rho}w\br{V}$. Let $H$ be the last component added to $A$ and let $A'$ be $A$ one iteration before that, i.e., $A'=A\setminus V_H$.
        Consider the two cases:
        \begin{itemize}
                        \item If $w\br{A', H}\geq \delta_{\rho}w\br{V}$, then $A=A'$ and $B=V\setminus A'$. We have
            \[
                        w\br{A, V}\geq w\br{A, B} \geq w\br{A', V_H}\geq \delta_{\rho}w\br{V}
            \]
                        and similarily, using $w\br{A,B}=w\br{B,A}$ for disjoint $A$ and $B$, $w\br{B, V}\geq \delta_{\rho}w\br{V}$.
            \item Otherwise, $A$ stays unchanged and we set $B=V\setminus A$. We have $w\br{A, V}\geq \delta_{\rho}w\br{V}$ due to the stop condition of the above greedy rule, and
              \begin{align*}
              w\br{B, V}
              &= 
              w\br{V}-w\br{A}= w\br{V}-\br{w\br{A'}+w\br{A', V_H}+w\br{V_H}}
              \\&\geq 
                            w\br{V}-3\delta_{\rho}w\br{V}\geq \delta_{\rho}w\br{V}.
            \end{align*}
                        where the last inequality follows from $\delta_{\rho}\leq 1/4$, again by $\delta_{\rho}=\min\brc{1/4,1-\rho}$.
        \end{itemize}
    \end{enumerate}
    This concludes the proof of the lemma.
  \end{proof}
    Take a partition $\br{A,B}$ as in Lemma~\ref{lem:ABpartition}.
        Suppose that we are at some iteration when $w\br{V\br{G'}}> \eta w\br{V}$. We have that
    \[
        w\br{A\cap V\br{G'}, V\br{G'}}\geq w\br{A, V}-w\br{V\setminus V\br{G'}}\geq \delta_{\rho}w\br{V}-\br{1-\eta}w\br{V}=\br{\eta+\delta_{\rho}-1}w\br{V}.
    \]
        Here we used the stopping condition $w\br{V\br{G'}}>\eta w\br{V}$, i.e. $w\br{V\setminus V\br{G'}}<\br{1-\eta}w\br{V}$.
        Symmetrically, for $B$ we get that, $w\br{B\cap V\br{G'}, V\br{G'}}\geq \br{\eta+\delta_{\rho}-1}w\br{V}$. Since $\eta>1-\delta_{\rho}$ by assumption on $\eta$, we have $\eta+\delta_{\rho}-1>0$. Let $\br{S, \bar{S}}$ be the cut returned by the $\gamma$-approximation algorithm for the \textsc{Minimum Generalized Conductance Problem} in $G'$. We have
\begin{align*}
        \frac{c\br{S, \bar{S}}}{w\br{S, V\br{G'}}\cdot w\br{\bar{S}, V\br{G'}}} & \leq \gamma \cdot \frac{c\br{A, B}}{w\br{A\cap V\br{G'}, V\br{G'}}\cdot w\br{B\cap V\br{G'}, V\br{G'}}} \\
        &\leq \frac{\gamma\cdot c\br{A, B}}{\br{\eta+\delta_{\rho}-1}^2w\br{V}^2}
\end{align*}

Now, by rearranging the above inequality we get:
\begin{align*}
        c\br{S, \bar{S}} & \leq \frac{\gamma\cdot c\br{A, B}}{\br{\eta+\delta_{\rho}-1}^2w\br{V}^2}\cdot w\br{S, V\br{G'}}\cdot w\br{\bar{S}, V\br{G'}} \\
        &\leq  \frac{\gamma\cdot c\br{C^*}\cdot w\br{S, V\br{G'}}}{\br{\eta+\delta_{\rho}-1}^2w\br{V}}
\end{align*}
since trivially we have $c\br{A, B}\leq c\br{C^*}$ and $w\br{\bar{S}, V\br{G'}}\leq w\br{V\br{G'}}\leq w\br{V}$.

Let $\ell$ be the index of some iteration, let $G_{\ell}'$ be $G'$ at this iteration and let $\br{S_{\ell}, \bar{S_{\ell}}}$ be the cut at this iteration. By summing the above inequality over all iterations we get:
\begin{align*}
    c\br{C} & \leq \sum_{\ell} c\br{S_{\ell}, \bar{S_{\ell}}} \leq \frac{\gamma\cdot c\br{C^*}}{\br{\eta+\delta_{\rho}-1}^2w\br{V}}\cdot \sum_{\ell} w\br{S_{\ell}, V\br{G'_{\ell}}} \\
    &\leq \frac{\gamma\cdot c\br{C^*}}{\br{\eta+\delta_{\rho}-1}^2w\br{V}}\cdot w\br{V} = \frac{\gamma}{\br{\eta+\delta_{\rho}-1}^2}\cdot c\br{C^*}
\end{align*}
where the last inequality follows since the sets $S_{\ell}$ are disjoint and thus $\sum_{\ell} w\br{S_{\ell}, V\br{G'_{\ell}}}\leq w\br{V}$.
\end{proof}

As an immediate corollary we get:
\begin{corollary}
    For fixed $\rho\in(0,1)$ and $\eta\in\br{\max\brc{1/2,1-\delta_{\rho}},1}$ (where $\delta_{\rho}=\min\brc{1/4,1-\rho}$), there is a pseudo-approximation algorithm for \textsc{Graph Partitioning with Demands} with the following guarantees:
    \[
    c\br{C}\leq O\br{\frac{\log n}{\br{\eta+\delta_{\rho}-1}^2}}\cdot c\br{C^*}
    \quad\text{on general graphs,}
    \]
    \[
    c\br{C}\leq O\br{\frac{1}{\br{\eta+\delta_{\rho}-1}^2}}\cdot c\br{C^*}
    \quad\text{on trees,}
    \]
    \[
    c\br{C}\leq O\br{\frac{\sqrt{\log n}}{\br{\eta+\delta_{\rho}-1}^2}}\cdot c\br{C^*}
    \quad\text{for multiplicative demands.}
    \]
    In all cases, every connected component $H$ of $G-C$ satisfies
    \[
    w\br{H}\leq \eta w\br{V}.
    \]
\end{corollary}

Observe that the above procedure does not work for all values of $\eta$: it requires $\eta>\max\brc{1/2,1-\delta_{\rho}}$. To remedy this issue we extend it into a recursive procedure that works for every $\eta\in(0,1)$.

Let $k'=\eta\,w\br{V}$ and $k=\rho\,w\br{V}$, and note that $k/k'=\rho/\eta$. Given a graph $H$, the recursive algorithm proceeds as follows:
\begin{enumerate}
    \item If $\spr{H}\leq k'$, return $\emptyset$.
    \item $\eta_0\gets \max\brc{4/5,\, k'/\spr{H}}$.
    \item Run the algorithm of Theorem~\ref{thm:gpd-bicriteria} on $H$ with parameter $\eta_0$ and let $C_H$ be the returned subset of edges.
    \item $C\gets C_H$.
    \item For every connected component $H'$ of $H-C_H$, recursively partition $H'$ and add the returned edges to $C$.
    \item Return $C$.
\end{enumerate}


\begin{theorem}\label{thm:gpd-extension}
    Fix $0<\rho\leq \eta< 1$, and let $
        \kappa\br{\rho,\eta}:=\max\brc{400,\ \frac{1}{\br{\br{1-\rho/\eta}\cdot 4/5}^2}}.$
    The recursive algorithm runs in polynomial time and returns a subset of edges $C$ such that every connected component $H$ of $G-C$ satisfies $w\br{H}\leq \eta\,w\br{V}$, and
    \[
        c\br{C}\leq d\cdot \kappa\br{\rho,\eta}\cdot \gamma\cdot c\br{C^*},
    \]
    where $d:=\cl{\log_{5/4}\br{1/\eta}}$, $C^*$ is the optimal solution and $\gamma$ is the approximation ratio for the \textsc{Minimum Generalized Conductance Problem}.
\end{theorem}
\begin{proof}
    It is easy to see that the algorithm runs in polynomial time. Hence, we focus on the approximation ratio.

    For a graph $H$ with $\spr{H}\geq k'$ we set $\eta_H:=k'/\spr{H}$ and $\rho_H:=k/\spr{H}$; observe that $\rho_H/\eta_H=k/k'=\rho/\eta$. We denote by $\OPT\br{H}$ the cost of the optimal \textsc{Graph Partitioning with Demands} solution on $H$ with threshold $\rho_H$, so that $\OPT\br{G}=c\br{C^*}$.

For a graph $H$, let $d\br{\spr{H}}$ denote the number of recursion levels the algorithm performs on $H$. Since every non-terminating recursive call is invoked with $\eta_0\geq 4/5$ and produces components of demand at most $\eta_0\spr{H}\leq \tfrac{4}{5}\spr{H}$, this also means that for every child $H'$ of $H$ in the recursion tree, we get $\eta_{H'}\geq 5/4\cdot \eta_H$. Because the last partitioning step happens when $\eta_H\geq 4/5$, the recursion depth is bounded by
\[
    d\br{\spr{H}}\leq \cl{\log_{5/4}\br{\spr{H}/k'}}.
\]
due to the fact after reaching $\eta_{H}\geq 4/5$ the algorithm perform one last partitioning step and a recursion call. Since $\log_{5/4}\br{\spr{H}/k'}$ is the amount of iterations required to reach $\eta_H\geq 1$, which is one more then we need to reach $\eta_H\geq 4/5$, we get that the above quantity is correct. 

Now, we prove by induction on $d\br{\spr{H}}$ that the recursive algorithm returns a subset of edges of cost at most $d\br{\spr{H}}\cdot \kappa\br{\rho,\eta}\cdot \gamma\cdot \OPT\br{H}$.

    \emph{Base cases, $d\br{\spr{H}}=0$ and $d\br{\spr{H}}=1$.} If $d\br{\spr{H}}=0$, then $\spr{H}\leq k'$, so the algorithm terminates without any partitioning, and the cost is $0$. If $d\br{\spr{H}}=1$, then $k'/\spr{H}\geq 4/5$, so $\eta_0=\eta_H=k'/\spr{H}$, and after a single partitioning step every component has demand at most $\eta_0\spr{H}=k'$; hence the algorithm terminates. By the explicit bound established in the proof of Theorem~\ref{thm:gpd-bicriteria}, this step has cost at most $\frac{\gamma}{\br{\eta_H+\delta_{\rho_H}-1}^2}\cdot \OPT\br{H}$. We bound $\frac{1}{\br{\eta_H+\delta_{\rho_H}-1}^2}\leq \kappa\br{\rho,\eta}$ in two cases:
    \begin{enumerate}
        \item If $\rho_H\leq 3/4$, then $\delta_{\rho_H}=1/4$ and $\eta_H+\delta_{\rho_H}-1\geq 4/5+1/4-1=1/20$, using $\eta_H\geq 4/5$. Hence $\frac{1}{\br{\eta_H+\delta_{\rho_H}-1}^2}\leq 400\leq \kappa\br{\rho,\eta}$.
        \item If $\rho_H> 3/4$, then $\delta_{\rho_H}=1-\rho_H$ and, using $\rho_H=\br{\rho/\eta}\eta_H$ and $\eta_H\geq 4/5$,
        \[
            \eta_H+\delta_{\rho_H}-1=\eta_H-\rho_H=\br{1-\rho/\eta}\eta_H\geq \br{1-\rho/\eta}\cdot\tfrac{4}{5}.
        \]
        Hence $\frac{1}{\br{\eta_H+\delta_{\rho_H}-1}^2}\leq \frac{1}{\br{\br{1-\rho/\eta}\cdot 4/5}^2}\leq \kappa\br{\rho,\eta}$.
    \end{enumerate}
    In both cases the cost is at most $\kappa\br{\rho,\eta}\cdot\gamma\cdot \OPT\br{H}=d\br{\spr{H}}\cdot \kappa\br{\rho,\eta}\cdot\gamma\cdot\OPT\br{H}$.

    \emph{Inductive step.} Suppose the claim holds for every graph $H'$ with $d\br{\spr{H'}}\leq d-1$, and let $H$ satisfy $d\br{\spr{H}}=d\geq 2$. Then $k'/\spr{H}<4/5$, so $\eta_0=4/5$, and every component $H'$ of $H-C_H$ has demand at most $\eta_0\spr{H}=\tfrac{4}{5}\spr{H}$, hence $d\br{\spr{H'}}\leq d-1$. The top-level partitioning step has cost at most $\frac{\gamma}{\br{\eta_0+\delta_{\rho_H}-1}^2}\cdot\OPT\br{H}$, and we again bound $\frac{1}{\br{\eta_0+\delta_{\rho_H}-1}^2}\leq \kappa\br{\rho,\eta}$:
    \begin{enumerate}
        \item If $\rho_H\leq 3/4$, then $\delta_{\rho_H}=1/4$ and $\eta_0+\delta_{\rho_H}-1=4/5+1/4-1=1/20$, so the term is at most $400\leq \kappa\br{\rho,\eta}$.
        \item If $\rho_H> 3/4$, then $\delta_{\rho_H}=1-\rho_H$ and, using $\rho_H=\br{\rho/\eta}\eta_H<\br{\rho/\eta}\cdot\tfrac{4}{5}=\br{\rho/\eta}\eta_0$,
        \[
            \eta_0+\delta_{\rho_H}-1=\eta_0-\rho_H> \eta_0-\br{\rho/\eta}\eta_0=\br{1-\rho/\eta}\cdot\tfrac{4}{5},
        \]
        so the term is at most $\frac{1}{\br{\br{1-\rho/\eta}\cdot 4/5}^2}\leq \kappa\br{\rho,\eta}$.
    \end{enumerate}
    Thus the top level contributes at most $\kappa\br{\rho,\eta}\cdot\gamma\cdot\OPT\br{H}$. By the inductive hypothesis, each recursive call on a component $H'$ contributes at most $\br{d-1}\cdot \kappa\br{\rho,\eta}\cdot\gamma\cdot\OPT\br{H'}$. Since the components $H'$ are vertex-disjoint, $\sum_{H'}\OPT\br{H'}\leq \OPT\br{H}$, and therefore
    \[
        c\br{C}\leq \kappa\br{\rho,\eta}\,\gamma\,\OPT\br{H}+\br{d-1}\,\kappa\br{\rho,\eta}\,\gamma\sum_{H'}\OPT\br{H'}\leq d\cdot \kappa\br{\rho,\eta}\cdot\gamma\cdot\OPT\br{H}.
    \]
    This proves the claim. Applying it to $H=G$, where $\spr{G}=w\br{V}$ and $k'=\eta\,w\br{V}$, gives $d\br{\spr{G}}\leq \cl{\log_{5/4}\br{1/\eta}}$, which yields the statement of the theorem.
\end{proof}

As an immediate corollary we get:
\begin{corollary}
    For fixed $\rho\in(0,1)$ and $\eta\in(0,1)$, there is an approximation algorithm for \textsc{Graph Partitioning with Demands} that returns a subset of edges $C$ such that every connected component $H$ of $G-C$ satisfies $w\br{H}\leq \eta\,w\br{V}$ and, writing $\Lambda:=d\cdot \kappa\br{\rho,\eta}$ with $d=\cl{\log_{5/4}\br{1/\eta}}$ and $\kappa\br{\rho,\eta}=\max\brc{400,\,1/\br{\br{1-\rho/\eta}\cdot4/5}^2}$, the following holds:
    \[
    c\br{C}\leq O\br{\Lambda\log n}\cdot c\br{C^*}
    \quad\text{on general graphs,}
    \]
    \[
    c\br{C}\leq O\br{\Lambda}\cdot c\br{C^*}
    \quad\text{on trees,}
    \]
    \[
    c\br{C}\leq O\br{\Lambda\sqrt{\log n}}\cdot c\br{C^*}
    \quad\text{for multiplicative demands.}
    \]
    In particular, for any fixed $\rho\in(0,1)$ and $\eta\in(0,1)$, thee value $\Lambda$ is a constant, and the above bounds are $O\br{\log n}$, $O\br{1}$ and $O\br{\sqrt{\log n}}$, respectively.
\end{corollary}


\subsection{Hierarchical clustering with demands}

In this problem one is given a graph $G=(V,E,c,w)$ with cost and demand functions $c\colon E\to \mathbb{N}$ and $w\colon V\to \mathbb{N}$, and the goal is to find a hierarchical clustering tree of $G$. The \emph{clustering tree} $T=(V_T,E_T)$ of $G$ is a binary tree where each node $u\in V_T$ is associated with a subset of $S_u\subseteq V$, that we called a \emph{cluster}, such that the subsets associated with children of $u$ form a partition of $S_u$.
The root's claster is $V$ and the leafs of $T$ are associated with singletons. The Dasgupta's clustering objective is to find a tree $T$ minimizing \[
c\br{T} = \sum_{u\in V\br{T}} w\br{S_u}\cdot c\br{S_u, V\setminus S_u}.
\]
We refer to the above problem as \textsc{Hierarchical Clustering with Demands}.
We show how to use the approximation algorithm for \textsc{Graph Partitioning with Demands} to obtain an approximation algorithm for \textsc{Hierarchical Clustering with Demands}. We note that the assumption of $T$ being binary is technical and can be relaxed due to an artibrary tree transformation into a binary one without increasing the cost.

The algorithm for \textsc{Hierarchical Clustering with Demands} is as follows:
\begin{enumerate}
 \item Let initially $T$ be a single (root) node associated with $V$.
 \item While $T$ has a node $v$ with $\spr{S_u}>1$, call a $\gamma$-approximation algorithm for \textsc{Graph Partitioning with Demands} on the graph induced by $S_u$ with $
\rho=1/2$ and $\eta=4/5.$. If $(S,S_u\setminus S)$ is the output, then introduce to $T$ two children with clusters $S$ and $S_u\setminus S$.
\end{enumerate}

In order to show that the above algorithm achieves an $O\br{\gamma}$-approximation ratio, where $\gamma$ is the approximation ratio for the \textsc{Graph Partitioning with Demands}, we use a generalized decomposition technique into levels due to \cite{ApproximateHierarchicalClusteringViaSparsestCutAndSpreadingMetrics}. Let $\cS_{t}$ be the set of all maximal clusters $S$ in $T^*$ such that $w\br{S}\leq t$. Observe that $\cS_t$ is a partition of $V$. Let $E_t^*$ be the set of all edges between clusters in $\cS_t$. For convenience let $E_0^*=E$. We call $E_t^*$ the \emph{cut at level} $t$. We have the following lemma:
\begin{lemma}
    $c\br{T^*} = \sum_{t=1}^{w\br{V}-1}c\br{E_t^*}$
\end{lemma}
\begin{proof}
    Consider any edge $uv$. Let $S$ be the minimal cluster in $T^*$ containing both $u$ and $v$. We get that the contribution of $uv$ to $c\br{T^*}$ is $w\br{S}\cdot c\br{uv}$. Moreover, we have that $uv\in E_t^*$ for every $t$ such that $t < w\br{S}$, so it contributes $c\br{uv}$ to $c\br{E_t^*}$ for every $t < w\br{S}$ which gives the claim.
\end{proof}

We also use the following upper bound on the $c\br{T^*}$ which is a direct consequence of the previous lemma:
\begin{corollary}
  It holds
    $2\cdot c\br{T^*} = 2\cdot \sum_{t=1}^{w\br{V}-1}c\br{E_t^*}\geq \sum_{t=0}^{w\br{V}}c\br{E_{\fl{t/2}}^*}$
\end{corollary}

Fix some non-leaf cluster $H$ in the tree $T$ computed by the algorithm and let $C_H$ be the cut returned by the \textsc{Graph Partitioning with Demands} algorithm with input $H$.
Let $r_H=w\br{H}$ and $s_H$ be the size of the largest child of $H$ with respect to the total demand.
With the above choice $\eta=4/5$, we have $s_H\leq 4r_H/5$. Edges cut in $H$ contribute $r_H\cdot c\br{C_H}$ to the objective function. We wish to charge this cost to the edges cut in $E_{r_H/2}^*$, restricted to $H$. We have the following bound:
\begin{align*}
    \br{r_H-s_H}\cdot c\br{E_{r_H/2}^*\cap H}\leq \sum_{t=s_H+1}^{r_H}c\br{E_{\fl{t/2}}^*\cap H}
\end{align*}
since as the level $t$ decreases, the more edges belong to $E_t^*$. 

The contribution of $C_H$ to the objective $c\br{T}$ to be at most:
\begin{align*}
    r_H\cdot c\br{C_H} \leq 5\cdot\br{r_H-s_H}\cdot c\br{E_{r_H/2}^*\cap H}\leq 5\cdot \sum_{t=s_H+1}^{r_H}c\br{E_{\fl{t/2}}^*\cap H}
\end{align*}
where the first inequality is due to the fact that $r_H-s_H\geq r_H/5$.

Now, since all of child clusters of $H$ have size at most $s_H$, they do not contribute to $c\br{E_{t}^*}$ for $t > s_H$. Combining this observation with the fact that the clusters in $T$ contributing to a given level $t$ form a partition of $V$, and thus are disjoint we get that:
\begin{align*}
    \sum_{H} \sum_{t=s_H+1}^{r_H}c\br{E_{t}^*\cap H} \leq \sum_{t=0}^{w\br{V}} c\br{E_{\fl{t/2}}^*}
\end{align*}

Thus by summing up over the conitribution of all cuts $C_H$ we get that the cost of the tree returned by the algorithm is at most:
\begin{align*}
    \sum_{H} r_H\cdot c\br{C_H} & \leq 5\cdot \sum_{H} \sum_{t=s_H+1}^{r_H}c\br{E_{t}^*\cap H} \\
    &\leq 5\cdot \sum_{t=0}^{w\br{V}} c\br{E_{\fl{t/2}}^*}\leq 10\cdot c\br{T^*}
\end{align*}
where the last inequality is by the previous corollary.

We get the following theorem:
\begin{theorem}
    The above algorithm achieves an $O\br{\gamma}$-approximation ratio, where $\gamma$ is the approximation ratio for the \textsc{Graph Partitioning with Demands}.
\end{theorem}

And as an immediate corollary we get:
\begin{corollary}
    There exists an $O\br{\log n}$-approximation algorithm for the \textsc{Hierarchical Clustering with Demands Problem} on general graphs and an $O\br{1}$-approximation algorithm for the \textsc{Hierarchical Clustering with Demands Problem} on trees. Moreover, if the demand function is multiplicative, then there exists an $O\br{\sqrt{\log n}}$-approximation algorithm.
\end{corollary}

